import 'package:flutter/material.dart';

class EmptyProgressDayWidget extends StatelessWidget {
  const EmptyProgressDayWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}